apiVersion: v1
kind: Service
metadata:
  name: pushgateway
  namespace: sunchipspace
spec:
  selector:
    app: pushgateway
  ports:
    - protocol: TCP
      port: {{ .Values.servicePort}}
      targetPort: {{ .Values.servicePort}}
  type: ClusterIP